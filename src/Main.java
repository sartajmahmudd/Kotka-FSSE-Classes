public class Main {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}

/*
Git commands from terminal/cmd

1. git init  (Make sure you are in the right project directory)
2. git add .  ( '.' represents all files)
3. git commit -m "My First Commit Message"
4. git branch -M master
5. git remote add origin < URL >
6. git push origin master


Git steps from IDE -> GUI
1. VCS -> Enable VCS
2. VCS -> Git -> Remote -> Add the repository URL
3. VCS -> Commit -> Select All the files -> Write a commit message -> press commit
4. VCS -> Git -> Branches -> New Branch -> create a new branch with name
5. VCS -> GIT -> Push (Make sure to check all the details of your commits)

 */